{% extends '@GlobalcomDoorman/layout.html.twig' %}

{% block title %}{{ 'createHouse'|trans({}, 'admin') }} - {{ parent() }}{% endblock %}

{% block body %}
    <h1>{{ 'createHouse'|trans({}, 'admin') }}</h1>

    {{ form_start(form, {action: path('admin_house_create')}) }}
        {{ form_widget(form) }}
        <p>
            <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'admin') }}</button>
        </p>
    {{ form_end(form) }}

        
        <div class="btn-group record_actions">

            <a class="btn btn-info" href="{{ path('admin_house') }}">
                <i class="fa fa-list"></i> {{ 'Back to the list'|trans({}, 'admin') }}
            </a>


        </div>


{% endblock %}
